--- STEP 9671 (replace_file_content) ---
StartLine: 1, EndLine: 10
TargetContent:
"import { CAAgentProvider } from \"@/components/agents-demo/CAAgentOrchestrator\";\nimport { useState, useEffect, useRef, useCallback, Suspense, lazy } from \"react\";\nimport { isCABackendConfigured } from \"@/lib/ca-backend-guard\";\nimport { getLiveRegulatoryNews, getStatutoryDeadlines } from \"@/services/ca-supabase-service\";\nimport { GST_TEMPLATE, MCA_TEMPLATE, IT_TEMPLATE, SEBI_TEMPLATE, RBI_TEMPLATE, CUSTOMS_TEMPLATE, LEGAL_TEMPLATE } from \"@/lib/draftTemplates\";\nconst MultiClientMasterHub = lazy(() => import(\"@/components/ca-dashboard-demo/MultiClientMasterHub\"));\nconst PracticeBillingPanel = lazy(() => import(\"@/components/ca-dashboard-demo/PracticeBillingPanel\"));\nconst SecureFileSharingPanel = lazy(() => import(\"@/components/ca-dashboard-demo/SecureFileSharingPanel\"));\nconst StatutoryDeadlineCalendar = lazy(() => import(\"@/components/ca-dashboard-demo/StatutoryDeadlineCalendar\"));\nimport { useNavigate } from \"react-router-dom\";"

ReplacementContent:
"import { CAAgentProvider } from \"@/components/agents-demo/CAAgentOrchestrator\";\nimport { useState, useEffect, useRef, useCallback, Suspense, lazy } from \"react\";\nimport { isCABackendConfigured } from \"@/lib/ca-backend-guard\";\nimport { getLiveRegulatoryNews, getStatutoryDeadlines } from \"@/services/ca-supabase-service\";\nimport { buildOfflineDraft, readyNoticeTemplates } from \"@/components/ca-dashboard-demo/AIDraftingEngine\";\nconst MultiClientMasterHub = lazy(() => import(\"@/components/ca-dashboard-demo/MultiClientMasterHub\"));\nconst PracticeBillingPanel = lazy(() => import(\"@/components/ca-dashboard-demo/PracticeBillingPanel\"));\nconst SecureFileSharingPanel = lazy(() => import(\"@/components/ca-dashboard-demo/SecureFileSharingPanel\"));\nconst StatutoryDeadlineCalendar = lazy(() => import(\"@/components/ca-dashboard-demo/StatutoryDeadlineCalendar\"));\nimport { useNavigate } from \"react-router-dom\";"

========================================
--- STEP 9675 (replace_file_content) ---
StartLine: 52, EndLine: 64
TargetContent:
"import {\n  Bot,\n  Building,\n  Clock,\n  AlertTriangle,\n  TrendingUp,\n  FileText,\n  Users,\n  Check,\n  CheckCircle,\n  MessageSquare,\n  BarChart3,\n  Plus,"

ReplacementContent:
"import {\n  Bot,\n  Building,\n  Clock,\n  AlertTriangle,\n  TrendingUp,\n  FileText,\n  Users,\n  Check,\n  CheckCircle,\n  MessageSquare,\n  BarChart3,\n  Plus,\n  Fingerprint,\n  Landmark,"

========================================
--- STEP 9679 (replace_file_content) ---
StartLine: 1027, EndLine: 1045
TargetContent:
"  // Document Upload\n  const [uploadedFile, setUploadedFile] = useState<File | null>(null);\n  const [isProcessing, setIsProcessing] = useState(false);\n  const [generatedDraft, setGeneratedDraft] = useState('');\n  const [draftApprovalStatus, setDraftApprovalStatus] = useState<'pending' | 'approved' | 'signed' | 'filed'>('pending');\n  const [isGeneratingPDF, setIsGeneratingPDF] = useState(false);\n  const [isSendingForSignature, setIsSendingForSignature] = useState(false);\n  const [isFilingToGovt, setIsFilingToGovt] = useState(false);\n  const [generatedPDFUrl, setGeneratedPDFUrl] = useState<string | null>(null);\n  const [signatureStatus, setSignatureStatus] = useState<string | null>(null);\n  const [filingARN, setFilingARN] = useState<string | null>(null);\n  \n  // New Fetched Digital Notices State\n  const [digitalNotices, setDigitalNotices] = useState<any[]>([]);\n  const [selectedDigitalNotice, setSelectedDigitalNotice] = useState<any>(null);\n  const [draftViewMode, setDraftViewMode] = useState<'text' | 'pdf'>('text');\n  \n  // Deployment\n  const [deploymentCommand, setDeploymentCommand] = useState('');\n  const [activeTab, setActiveTab] = useState('dashboard');"

ReplacementContent:
"  // Document Upload\n  const [uploadedFile, setUploadedFile] = useState<File | null>(null);\n  const [isProcessing, setIsProcessing] = useState(false);\n  const [generatedDraft, setGeneratedDraft] = useState('');\n  const [draftApprovalStatus, setDraftApprovalStatus] = useState<'pending' | 'approved' | 'signed' | 'filed'>('pending');\n  const [isGeneratingPDF, setIsGeneratingPDF] = useState(false);\n  const [isSendingForSignature, setIsSendingForSignature] = useState(false);\n  const [isFilingToGovt, setIsFilingToGovt] = useState(false);\n  const [generatedPDFUrl, setGeneratedPDFUrl] = useState<string | null>(null);\n  const [signatureStatus, setSignatureStatus] = useState<string | null>(null);\n  const [filingARN, setFilingARN] = useState<string | null>(null);\n  \n  // Interactive Workflow Modals State\n  const [showOtpModal, setShowOtpModal] = useState(false);\n  const [otpInput, setOtpInput] = useState('');\n  const [otpVerifying, setOtpVerifying] = useState(false);\n  const [showFilingConsole, setShowFilingConsole] = useState(false);\n  const [filingLogs, setFilingLogs] = useState<string[]>([]);\n  const [showFilingReceipt, setShowFilingReceipt] = useState(false);\n  \n  // Current Draft Notice Data\n  const [currentDraftClient, setCurrentDraftClient] = useState('GlobalTrade India Logistics');\n  const [currentDraftType, setCurrentDraftType] = useState('gst-show-cause');\n  const [currentDraftRef, setCurrentDraftRef] = useState('ZD070226019874A');\n  const [currentDraftMismatch, setCurrentDraftMismatch] = useState('₹18,46,920');\n  const [currentDraftPortal, setCurrentDraftPortal] = useState('GST Portal');\n  \n  // New Fetched Digital Notices State (Populated by default with 3 notices)\n  const [digitalNotices, setDigitalNotices] = useState<any[]>([\n    {\n      id: 'not-dig-1',\n      refNumber: 'ZD070226019874A',\n      client: 'GlobalTrade India Logistics',\n      portal: 'GST Portal',\n      noticeType: 'GST Show Cause Notice (GST-DRC-01)',\n      issueDate: '2026-02-07',\n      dueDate: '2026-06-2
<truncated 1086 bytes>

========================================
--- STEP 9683 (replace_file_content) ---
StartLine: 1109, EndLine: 1114
TargetContent:
"  // Fetch Client Deadlines — calculated dynamically from today's date\n  const fetchClientDeadlines = async () => {\n    const deadlines = getStatutoryDeadlines();\n    setClientDeadlines(deadlines as any);\n    addAgentLog('📅 Statutory deadlines calculated for May–June 2026');\n  };"

ReplacementContent:
"  // Fetch Client Deadlines — calculated dynamically from today's date\n  const fetchClientDeadlines = async () => {\n    const deadlines = getStatutoryDeadlines();\n    const mockClients = [\n      \"GlobalTrade India Logistics\",\n      \"Acme Technologies Pvt Ltd\",\n      \"TechVenture Solutions\",\n      \"GlobalTrade India Logistics\",\n      \"Acme Technologies Pvt Ltd\",\n      \"TechVenture Solutions\"\n    ];\n    const deadlinesWithClients = deadlines.map((d, index) => ({\n      ...d,\n      client: mockClients[index % mockClients.length]\n    }));\n    setClientDeadlines(deadlinesWithClients as any);\n    addAgentLog('📅 Statutory deadlines calculated for May–June 2026');\n  };"

========================================
--- STEP 9689 (replace_file_content) ---
StartLine: 1148, EndLine: 1208
TargetContent:
"  // Deploy AI Agent for Task\n  const deployAgentForTask = async (taskType: AITask['type'], client: string, description: string) => {\n    const newTask: AITask = {\n      id: `task-${Date.now()}`,\n      type: taskType,\n      title: getTaskTitle(taskType),\n      client,\n      status: 'queued',\n      progress: 0,\n      createdAt: new Date().toISOString(),\n      description\n    };\n    \n    setAiTasks(prev => [newTask, ...prev]);\n    setAgentStatus('working');\n    addAgentLog(`🚀 Deploying AI Agent for ${newTask.title} - ${client}`);\n    \n    // Simulate AI processing\n    await processTask(newTask);\n  };\n \n   // Process Task\n   const processTask = async (task: AITask) => {\n     // Update to processing\n     setAiTasks(prev => prev.map(t => \n       t.id === task.id ? { ...t, status: 'processing', progress: 10 } : t\n     ));\n     addAgentLog(`⚙️ Processing: ${task.title} for ${task.client}`);\n     \n     // Simulate progressive work\n     const steps = [\n       { progress: 25, log: `📊 Fetching data for ${task.client}...` },\n       { progress: 40, log: `🔍 Analyzing compliance requirements...` },\n       { progress: 60, log: `📝 Generating draft documents...` },\n       { progress: 80, log: `✅ Validating against regulatory standards...` },\n       { progress: 95, log: `📋 Preparing for CA review and approval...` },\n     ];\n     \n     for (const step of steps) {\n       await new Promise(resolve => setTimeout(resolve, 1000));\n       setAiTasks(prev => prev.map(t => \n         t.id === task.id ? { ...t, progress: step.progress } : t\n       ));\n       addAgentLog(step.log);\n     }\n     \n     // Complete processing, await approval\n     setAiTasks(prev => prev.map(t => \n       t.id === task.id ? { \n         ...t, \n         status: 'pending_approval', \n         progress: 100,\n         result: generateTaskResult(task.type, task.client)\n       } : t\n     ));\n     setAgentStatus('awaiting_approval');\n     addAgentLog(`⏳ ${task.title} ready for CA approval`);\n   
<truncated 146 bytes>

ReplacementContent:
"  // Deploy AI Agent for Task\n  const deployAgentForTask = async (taskType: AITask['type'], client: string, description: string) => {\n    // Determine mapping to statutory templates\n    let noticeKey = 'custom-draft';\n    let portal = 'GST Portal';\n    let refNum = `SCN-DRC-ZD070226019874A`;\n    let mismatch = `₹18,46,920`;\n\n    const descLower = description.toLowerCase();\n    const typeLower = taskType.toLowerCase();\n    \n    if (typeLower === 'reconciliation' || descLower.includes('gst') || descLower.includes('gstr')) {\n      noticeKey = 'gst-show-cause';\n      portal = 'GST Portal';\n      refNum = 'ZD070226019874A';\n      mismatch = '₹18,46,920';\n    } else if (descLower.includes('mca') || descLower.includes('roc') || descLower.includes('annual filing') || descLower.includes('aoc') || descLower.includes('mgt')) {\n      noticeKey = 'mca-notice';\n      portal = 'MCA Portal';\n      refNum = 'ROC/KA/ADJ/2026/112';\n      mismatch = '₹5,00,000';\n    } else if (descLower.includes('income') || descLower.includes('tax') || descLower.includes('scrutiny') || descLower.includes('143')) {\n      noticeKey = 'income-tax-response';\n      portal = 'Income Tax Portal';\n      refNum = 'ITBA/AST/S/143(2)/2026-27';\n      mismatch = '₹27,80,000';\n    } else if (descLower.includes('rbi') || descLower.includes('fema')) {\n      noticeKey = 'rbi-filing';\n      portal = 'RBI Portal';\n      refNum = 'RBI/FED/2026-27/245';\n      mismatch = '₹12,00,000';\n    } else if (descLower.includes('sebi')) {\n      noticeKey = 'sebi-compliance';\n      portal = 'SEBI Portal';\n      refNum = 'SEBI/HO/CFD/SCN/2026/311';\n      mismatch = '₹15,00,000';\n    }\n\n    setCurrentDraftClient(client);\n    setCurrentDraftType(noticeKey);\n    setCurrentDraftRef(refNum);\n    setCurrentDraftPortal(portal);\n    setCurrentDraftMismatch(mismatch);\n    setDraftApprovalStatus('pending');\n    setSignatureStatus(null);\n    setFilingARN(null);\n\n    const newTask: AITask = {\n      id: `task-${Date.now()}`,\n      ty
<truncated 2868 bytes>

========================================
--- STEP 9697 (replace_file_content) ---
StartLine: 1407, EndLine: 1567
TargetContent:
"  // Process Uploaded Document\n  const processUploadedDocument = async () => {\n    if (!uploadedFile) return;\n    \n    setIsProcessing(true);\n    addAgentLog(`🔄 Uploading document: ${uploadedFile.name}`);\n    \n    try {\n      await new Promise(resolve => setTimeout(resolve, 800));\n      addAgentLog(`📝 Reading text from uploaded notice: ${uploadedFile.name}...`);\n      \n      await new Promise(resolve => setTimeout(resolve, 1000));\n      addAgentLog(`👁️ SANNIDH Vision OCR Engine initialized successfully.`);\n      addAgentLog(`✅ OCR extracted 3,412 characters from scanned document.`);\n      \n      await new Promise(resolve => setTimeout(resolve, 1200));\n      addAgentLog(`🏦 Registering GST Notice SCN-829412 in SANNIDH Database...`);\n      addAgentLog(`🤖 SANNIDH AI Brain (OpenAI) cross-referencing ledger database & CBIC rules...`);\n      \n      await new Promise(resolve => setTimeout(resolve, 1500));\n      let content = `\nBEFORE THE CENTRAL GOODS AND SERVICES TAX OFFICERS, DIVISION-I, NEW DELHI\n\nIN THE MATTER OF:\nGlobalTrade India Logistics (Assessee)\nGSTIN: 27AABCG5678K2ZQ\n\nSUBJECT: REPLY TO SHOW CAUSE NOTICE REF NO: SCN-829412 DATED 25/05/2026\n\nMOST RESPECTFULLY SHOWETH:\n\n1. PRELIMINARY OBJECTIONS\nThe Assessee, GlobalTrade India Logistics, submits that the impugned Show Cause Notice proposing demand of Input Tax Credit (ITC) mismatch under GSTR-2B vs GSTR-3B is legally untenable and contrary to established circulars.\n\n2. FACTUAL MATRIX & RECONCILIATION\nThe discrepancy amount of ₹2,40,000 alleged in the SCN has been thoroughly reconciled. The mismatch arose primarily due to delayed filing by suppliers and genuine clerical errors which do not indicate any intent to evade tax.\n\n3. LEGAL DEFENSES UNDER GST LAW\nIt is a settled principle that the substantive right to claim ITC under Section 16 of the CGST Act cannot be denied merely on account of procedural lapses.\n\nPRAYER\nIn view of the above submissions, it is prayed that the demand proposed in the impugne
<truncated 5311 bytes>

ReplacementContent:
"  // Process Uploaded Document\n  const processUploadedDocument = async () => {\n    if (!uploadedFile) return;\n    \n    setIsProcessing(true);\n    addAgentLog(`🔄 Uploading document: ${uploadedFile.name}`);\n    setActiveTab('drafting');\n    \n    try {\n      await new Promise(resolve => setTimeout(resolve, 800));\n      addAgentLog(`📝 Reading text from uploaded notice: ${uploadedFile.name}...`);\n      \n      await new Promise(resolve => setTimeout(resolve, 1000));\n      addAgentLog(`👁️ SANNIDH Vision OCR Engine initialized successfully.`);\n      addAgentLog(`✅ OCR extracted 3,412 characters from scanned document.`);\n      \n      await new Promise(resolve => setTimeout(resolve, 1200));\n      addAgentLog(`🏦 Registering GST Notice SCN-829412 in SANNIDH Database...`);\n      addAgentLog(`🤖 SANNIDH AI Brain (OpenAI) cross-referencing ledger database & CBIC rules...`);\n      \n      await new Promise(resolve => setTimeout(resolve, 1000));\n\n      const client = \"GlobalTrade India Logistics\";\n      const noticeKey = \"gst-show-cause\";\n      const portal = \"GST Portal\";\n      const refNum = \"ZD070226019874A\";\n      const mismatch = \"₹18,46,920\";\n\n      setCurrentDraftClient(client);\n      setCurrentDraftType(noticeKey);\n      setCurrentDraftRef(refNum);\n      setCurrentDraftPortal(portal);\n      setCurrentDraftMismatch(mismatch);\n      setDraftApprovalStatus('pending');\n      setSignatureStatus(null);\n      setFilingARN(null);\n\n      const draftText = buildOfflineDraft({\n        documentType: noticeKey,\n        companyName: client,\n        authority: \"GST Department\",\n        noticeText: readyNoticeTemplates[noticeKey] || \"\",\n        modeLabel: \"conservative\",\n        templatePack: \"Detailed Para-Wise Reply\",\n        promptPack: \"Precedent Analysis + Objection Matrix\",\n        sovereignEngine: \"sannidh_sovereign\"\n      });\n\n      setGeneratedDraft(draftText);\n      setDraftViewMode('pdf');\n      \n      addAgentLog(`✅ Real Legal Draft
<truncated 4832 bytes>

========================================
--- STEP 9701 (replace_file_content) ---
StartLine: 1564, EndLine: 1823
TargetContent:
"  const handleApproveAndGeneratePDF = async () => {\n    if (!generatedDraft.trim()) {\n      toast.error('No draft to approve.');\n      return;\n    }\n    setIsGeneratingPDF(true);\n    addAgentLog('📄 Generating Professional PDF with Firm Letterhead...');\n    \n    try {\n      const { jsPDF } = await import('jspdf');\n      const doc = new jsPDF({ orientation: 'portrait', unit: 'mm', format: 'a4' });\n      const pageWidth = doc.internal.pageSize.getWidth();\n      const pageHeight = doc.internal.pageSize.getHeight();\n      const margin = 20;\n      const contentWidth = pageWidth - (margin * 2);\n      let yPos = margin;\n\n      // ── FIRM LETTERHEAD HEADER ──\n      doc.setFillColor(15, 23, 42); // Dark navy\n      doc.rect(0, 0, pageWidth, 38, 'F');\n      \n      // Firm Name\n      doc.setTextColor(56, 189, 248); // Cyan\n      doc.setFontSize(18);\n      doc.setFont('helvetica', 'bold');\n      doc.text('SANNIDH', margin, 15);\n      \n      // Subtitle\n      doc.setTextColor(148, 163, 184); // Slate\n      doc.setFontSize(8);\n      doc.setFont('helvetica', 'normal');\n      doc.text('Chartered Accountants & Legal Advisors | Autonomous Compliance Platform', margin, 22);\n      doc.text('GSTIN: 07AXXXX1234Z1ZP | PAN: AXXXX1234Z | ICAI Reg: XXXXXX', margin, 27);\n      \n      // Confidential stamp\n      doc.setTextColor(239, 68, 68); // Red\n      doc.setFontSize(7);\n      doc.setFont('helvetica', 'bold');\n      doc.text('PRIVILEGED & CONFIDENTIAL', pageWidth - margin, 15, { align: 'right' });\n      doc.text('ATTORNEY-CLIENT PRIVILEGE', pageWidth - margin, 20, { align: 'right' });\n      \n      // Divider line\n      doc.setDrawColor(56, 189, 248);\n      doc.setLineWidth(0.5);\n      doc.line(margin, 39, pageWidth - margin, 39);\n      \n      yPos = 48;\n\n      // ── DOCUMENT METADATA ──\n      doc.setTextColor(30, 41, 59);\n      doc.setFontSize(13);\n      doc.setFont('helvetica', 'bold');\n      doc.text('LEGAL RESPONSE TO GOVERNMENT NOTICE', margin, yPos);\n      yPos 
<truncated 8630 bytes>

ReplacementContent:
"  const handleApproveAndGeneratePDF = async () => {\n    if (!generatedDraft.trim()) {\n      toast.error('No draft to approve.');\n      return;\n    }\n    setIsGeneratingPDF(true);\n    addAgentLog('📄 Generating Professional PDF with Firm Letterhead...');\n    \n    try {\n      const { jsPDF } = await import('jspdf');\n      const doc = new jsPDF({ orientation: 'portrait', unit: 'mm', format: 'a4' });\n      const pageWidth = doc.internal.pageSize.getWidth();\n      const pageHeight = doc.internal.pageSize.getHeight();\n      const margin = 20;\n      const contentWidth = pageWidth - (margin * 2);\n      let yPos = margin;\n\n      // ── FIRM LETTERHEAD HEADER ──\n      doc.setFillColor(15, 23, 42); // Dark navy\n      doc.rect(0, 0, pageWidth, 38, 'F');\n      \n      // Firm Name\n      doc.setTextColor(56, 189, 248); // Cyan\n      doc.setFontSize(18);\n      doc.setFont('helvetica', 'bold');\n      doc.text('SANNIDH & CO.', margin, 15);\n      \n      // Subtitle\n      doc.setTextColor(148, 163, 184); // Slate\n      doc.setFontSize(8);\n      doc.setFont('helvetica', 'normal');\n      doc.text('Chartered Accountants & Legal Advisors | Autonomous Compliance Platform', margin, 22);\n      doc.text('GSTIN: 07AXXXX1234Z1ZP | PAN: AXXXX1234Z | ICAI Reg: 1029481W', margin, 27);\n      \n      // Confidential stamp\n      doc.setTextColor(239, 68, 68); // Red\n      doc.setFontSize(7);\n      doc.setFont('helvetica', 'bold');\n      doc.text('PRIVILEGED & CONFIDENTIAL', pageWidth - margin, 15, { align: 'right' });\n      doc.text('ATTORNEY-CLIENT PRIVILEGE', pageWidth - margin, 20, { align: 'right' });\n      \n      // Divider line\n      doc.setDrawColor(56, 189, 248);\n      doc.setLineWidth(0.5);\n      doc.line(margin, 39, pageWidth - margin, 39);\n      \n      yPos = 48;\n\n      // ── DOCUMENT METADATA ──\n      doc.setTextColor(30, 41, 59);\n      doc.setFontSize(13);\n      doc.setFont('helvetica', 'bold');\n      doc.text('LEGAL RESPONSE TO GOVERNMENT NOTICE', margin, yPos);\n   
<truncated 10234 bytes>

========================================
--- STEP 9709 (replace_file_content) ---
StartLine: 1332, EndLine: 1368
TargetContent:
"  // Approve Task\n  const approveTask = async (taskId: string) => {\n    addAgentLog(`✅ CA APPROVED: Task ${taskId}`);\n    const task = aiTasks.find(t => t.id === taskId);\n    setAiTasks(prev => prev.map(t =>\n      t.id === taskId ? { ...t, status: 'approved' } : t\n    ));\n\n    // Log WORM audit entry for legal accountability\n    if (task) {\n      const { logWORMAuditEntry } = await import('@/services/ca-supabase-service');\n      await logWORMAuditEntry({\n        draftContent: JSON.stringify(task.result || task.description),\n        documentType: task.type,\n        clientName: task.client,\n        caAction: 'approved',\n      });\n      addAgentLog(`🔐 WORM Audit entry logged — SHA-256 hash stored in Supabase`);\n    }\n\n    // Generate final PDF\n    await new Promise(resolve => setTimeout(resolve, 1500));\n    addAgentLog(`📄 Generating final PDF with SANNIDH AI seal...`);\n    \n    await new Promise(resolve => setTimeout(resolve, 1000));\n    setAiTasks(prev => prev.map(t => \n      t.id === taskId ? { ...t, status: 'completed' } : t\n    ));\n    addAgentLog(`✨ Task completed and PDF ready for download`);\n    \n    setShowApprovalModal(false);\n    setSelectedTask(null);\n    setAgentStatus('monitoring');\n    \n    toast.success('Document approved and finalized!', {\n      description: 'PDF with SANNIDH AI seal is ready for download'\n    });\n  };"

ReplacementContent:
"  // Approve Task\n  const approveTask = async (taskId: string) => {\n    addAgentLog(`✅ CA APPROVED: Task ${taskId}`);\n    const task = aiTasks.find(t => t.id === taskId);\n    setAiTasks(prev => prev.map(t =>\n      t.id === taskId ? { ...t, status: 'approved' } : t\n    ));\n\n    if (task) {\n      const { logWORMAuditEntry } = await import('@/services/ca-supabase-service');\n      await logWORMAuditEntry({\n        draftContent: JSON.stringify(task.result || task.description),\n        documentType: task.type,\n        clientName: task.client,\n        caAction: 'approved',\n      });\n      addAgentLog(`🔐 WORM Audit entry logged — SHA-256 hash stored in Supabase`);\n      \n      // Map and load the draft for the approved task!\n      let noticeKey = 'custom-draft';\n      let portal = 'GST Portal';\n      let refNum = `SCN-DRC-ZD070226019874A`;\n      let mismatch = `₹18,46,920`;\n\n      const titleLower = task.title.toLowerCase();\n      const descLower = task.description.toLowerCase();\n      \n      if (titleLower.includes('gst') || descLower.includes('gst') || titleLower.includes('reconcile')) {\n        noticeKey = 'gst-show-cause';\n        portal = 'GST Portal';\n        refNum = 'ZD070226019874A';\n        mismatch = '₹18,46,920';\n      } else if (titleLower.includes('mca') || descLower.includes('mca') || titleLower.includes('aoc') || titleLower.includes('mgt')) {\n        noticeKey = 'mca-notice';\n        portal = 'MCA Portal';\n        refNum = 'ROC/KA/ADJ/2026/112';\n        mismatch = '₹5,00,000';\n      } else if (titleLower.includes('income') || descLower.includes('income') || titleLower.includes('143')) {\n        noticeKey = 'income-tax-response';\n        portal = 'Income Tax Portal';\n        refNum = 'ITBA/AST/S/143(2)/2026-27';\n        mismatch = '₹27,80,000';\n      }\n\n      setCurrentDraftClient(task.client);\n      setCurrentDraftType(noticeKey);\n      setCurrentDraftRef(refNum);\n      setCurrentDraftPortal(portal);\n      setCurrentDraftMismatch(mismatch);\n
<truncated 1451 bytes>

========================================
--- STEP 9715 (replace_file_content) ---
StartLine: 2708, EndLine: 2712
TargetContent:
"        )}\n      </AnimatePresence>\n    </motion.div>"

ReplacementContent:
"        )}\n      </AnimatePresence>\n\n      {/* ─── AADHAAR OTP INTERACTIVE E-SIGN MODAL ─── */}\n      <Dialog open={showOtpModal} onOpenChange={setShowOtpModal}>\n        <DialogContent className=\"max-w-md bg-zinc-950 border border-cyan-500/30 text-white p-6 rounded-2xl\">\n          <DialogHeader>\n            <DialogTitle className=\"flex items-center gap-2 text-xl text-cyan-400\">\n              <Fingerprint className=\"w-6 h-6 text-cyan-400 animate-pulse\" />\n              Secure Aadhaar e-Sign Verification\n            </DialogTitle>\n            <DialogDescription className=\"text-zinc-400 text-xs mt-1\">\n              Cryptographic authentication via UIDAI Government server for client authorization handoff.\n            </DialogDescription>\n          </DialogHeader>\n\n          <div className=\"space-y-4 my-4\">\n            <div className=\"p-3 bg-cyan-950/20 border border-cyan-500/20 rounded-xl space-y-1 text-xs text-left\">\n              <p><span className=\"text-zinc-400 font-semibold\">Signatory:</span> Director, {currentDraftClient}</p>\n              <p><span className=\"text-zinc-400 font-semibold\">Document:</span> SANNIDH-Legal-Response-{currentDraftRef}.pdf</p>\n              <p><span className=\"text-zinc-400 font-semibold\">Aadhaar Link:</span> XXXX-XXXX-4928</p>\n              <p><span className=\"text-zinc-400 font-semibold\">Mobile OTP:</span> Sent to registered mobile ending in 5829</p>\n            </div>\n\n            <div className=\"space-y-2 text-left\">\n              <label className=\"text-xs font-semibold text-zinc-400 block\">Enter 6-Digit Aadhaar OTP</label>\n              <Input\n                placeholder=\"Enter 6-digit OTP (e.g. 123456)\"\n                value={otpInput}\n                onChange={(e) => setOtpInput(e.target.value)}\n                className=\"bg-zinc-900 border-border/50 font-mono text-center text-lg tracking-widest text-white\"\n                maxLength={6}\n              />\n              <span className=\"text-[10px] text-zinc
<truncated 8155 bytes>

========================================
--- STEP 9721 (replace_file_content) ---
StartLine: 2278, EndLine: 2281
TargetContent:
"                    </div>\n                  </div>\n\n                </div>"

ReplacementContent:
"                    </div>\n                  </div>\n\n                  {/* Divider */}\n                  <div className=\"border-t border-border/20 my-4\" />\n\n                  {/* Scanned Notice Uploader */}\n                  <div className=\"space-y-3\">\n                    <h3 className=\"text-sm font-semibold text-cyan-300 flex items-center\">\n                      <Upload className=\"w-4 h-4 mr-2 text-cyan-400\" />\n                      Or Upload Scanned Physical Notice\n                    </h3>\n                    <p className=\"text-xs text-muted-foreground\">\n                      Upload scanned notice images or PDF. Sannidh OCR Swarm will extract text and link to data room registers.\n                    </p>\n                    \n                    <div className=\"border-2 border-dashed border-cyan-500/20 hover:border-cyan-500/40 rounded-xl p-4 transition-all duration-300 bg-card/20 text-center relative cursor-pointer group\">\n                      <input \n                        type=\"file\" \n                        id=\"notice-file-upload-input\"\n                        className=\"hidden\" \n                        onChange={handleFileUpload} \n                        accept=\".pdf,.png,.jpg,.jpeg\"\n                      />\n                      <label htmlFor=\"notice-file-upload-input\" className=\"cursor-pointer space-y-2 block\">\n                        <Upload className=\"w-8 h-8 text-cyan-500/60 mx-auto group-hover:scale-110 transition-transform duration-300\" />\n                        <div className=\"space-y-1\">\n                          <p className=\"text-xs font-semibold text-white\">\n                            {uploadedFile ? uploadedFile.name : \"Drag notice PDF or image here, or browse\"}\n                          </p>\n                          <p className=\"text-[10px] text-zinc-500\">\n                            Supports PDF, PNG, JPG up to 10MB\n                          </p>\n                        </div>\n                      </label>\n         
<truncated 580 bytes>

========================================
--- STEP 9729 (replace_file_content) ---
StartLine: 3006, EndLine: 3008
TargetContent:
"  useEffect(() => {\n    // Bypassed polling to preserve simulated companies state\n  }, []);"

ReplacementContent:
"  useEffect(() => {\n    // Seed default clients for premium CA Demo dashboard experience if empty\n    const saved = localStorage.getItem('demo_clients');\n    if (!saved || JSON.parse(saved).length === 0) {\n      const defaultClients = [\n        {\n          id: \"demo-client-1\",\n          name: \"GlobalTrade India Logistics\",\n          industry: \"Transportation & Logistics\",\n          gstin: \"27AABCG5678K2ZQ\",\n          pan: \"AABCG5678K\",\n          cin: \"U60230DL2021PTC389412\",\n          compliance_health: 78,\n          health: 78,\n          status: \"Active\",\n          risk_level: \"Medium\",\n          lastFiling: \"2026-05-15\",\n          pending_tasks: 2\n        },\n        {\n          id: \"demo-client-2\",\n          name: \"Acme Technologies Pvt Ltd\",\n          industry: \"Technology & Software\",\n          gstin: \"29AABCA1234Q1Z2\",\n          pan: \"AABCA1234Q\",\n          cin: \"U72900KA2022PTC159048\",\n          compliance_health: 92,\n          health: 92,\n          status: \"Active\",\n          risk_level: \"Low\",\n          lastFiling: \"2026-05-20\",\n          pending_tasks: 1\n        },\n        {\n          id: \"demo-client-3\",\n          name: \"TechVenture Solutions\",\n          industry: \"Professional Services\",\n          gstin: \"07AABCT9876E1Z4\",\n          pan: \"AABCT9876E\",\n          cin: \"U72200DL2023PTC390218\",\n          compliance_health: 64,\n          health: 64,\n          status: \"Active\",\n          risk_level: \"High\",\n          lastFiling: \"2026-05-10\",\n          pending_tasks: 3\n        }\n      ];\n      localStorage.setItem('demo_clients', JSON.stringify(defaultClients));\n      window.dispatchEvent(new CustomEvent('demo-client-added'));\n      window.dispatchEvent(new CustomEvent('ca:metrics-updated'));\n    }\n  }, []);"

========================================
--- STEP 9737 (replace_file_content) ---
StartLine: 2427, EndLine: 2430
TargetContent:
"                              <div className=\"my-2 p-1.5 rounded bg-blue-500/10 border border-blue-500/30 text-blue-700 max-w-[200px] text-left\">\n                                <p className=\"font-serif italic text-xs font-bold text-blue-850\">GlobalTrade Director</p>\n                                <p className=\"text-[7px] font-sans text-blue-600\">Cryptographic Signature Attached<br />ID Verified via AADHAR OTP Authentication</p>\n                              </div>"

ReplacementContent:
"                              <div className=\"my-2 p-1.5 rounded bg-blue-500/10 border border-blue-500/30 text-blue-700 max-w-[200px] text-left\">\n                                <p className=\"font-serif italic text-xs font-bold text-blue-850\">{currentDraftClient.includes(\"GlobalTrade\") ? \"GlobalTrade Director\" : currentDraftClient.includes(\"Acme\") ? \"Acme Director\" : \"TechVenture Director\"}</p>\n                                <p className=\"text-[7px] font-sans text-blue-600\">Cryptographic Signature Attached<br />ID Verified via AADHAR OTP Authentication</p>\n                              </div>"

========================================
--- STEP 9759 (replace_file_content) ---
StartLine: 50, EndLine: 52
TargetContent:
"import { Progress } from \"@/components/ui/progress\";\nimport { Tabs, TabsContent, TabsList, TabsTrigger } from \"@/components/ui/tabs\";"

ReplacementContent:
"import { Progress } from \"@/components/ui/progress\";\nimport { Tabs, TabsContent, TabsList, TabsTrigger } from \"@/components/ui/tabs\";\nimport { Dialog, DialogContent, DialogHeader, DialogTitle, DialogDescription } from \"@/components/ui/dialog\";"

========================================
--- STEP 9881 (replace_file_content) ---
StartLine: 1047, EndLine: 1092
TargetContent:
"  // Current Draft Notice Data\n  const [currentDraftClient, setCurrentDraftClient] = useState('GlobalTrade India Logistics');\n  const [currentDraftType, setCurrentDraftType] = useState('gst-show-cause');\n  const [currentDraftRef, setCurrentDraftRef] = useState('ZD070226019874A');\n  const [currentDraftMismatch, setCurrentDraftMismatch] = useState('₹18,46,920');\n  const [currentDraftPortal, setCurrentDraftPortal] = useState('GST Portal');\n  \n  // New Fetched Digital Notices State (Populated by default with 3 notices)\n  const [digitalNotices, setDigitalNotices] = useState<any[]>([\n    {\n      id: 'not-dig-1',\n      refNumber: 'ZD070226019874A',\n      client: 'GlobalTrade India Logistics',\n      portal: 'GST Portal',\n      noticeType: 'GST Show Cause Notice (GST-DRC-01)',\n      issueDate: '2026-02-07',\n      dueDate: '2026-06-25',\n      mismatchAmount: '₹18,46,920',\n      status: 'action_required'\n    },\n    {\n      id: 'not-dig-2',\n      refNumber: 'ROC/KA/ADJ/2026/112',\n      client: 'Acme Technologies Pvt Ltd',\n      portal: 'MCA Portal',\n      noticeType: 'MCA Adjudication Notice (ROC)',\n      issueDate: '2026-01-15',\n      dueDate: '2026-06-30',\n      mismatchAmount: '₹5,00,000',\n      status: 'action_required'\n    },\n    {\n      id: 'not-dig-3',\n      refNumber: 'ITBA/AST/S/143(2)/2026-27',\n      client: 'TechVenture Solutions',\n      portal: 'Income Tax Portal',\n      noticeType: 'Section 143(2) Scrutiny Notice',\n      issueDate: '2026-06-05',\n      dueDate: '2026-07-10',\n      mismatchAmount: '₹27,80,000',\n      status: 'action_required'\n    }\n  ]);\n  const [selectedDigitalNotice, setSelectedDigitalNotice] = useState<any>(null);\n  const [draftViewMode, setDraftViewMode] = useState<'text' | 'pdf'>('text');"

ReplacementContent:
"  // Current Draft Notice Data\n  const [currentDraftClient, setCurrentDraftClient] = useState('');\n  const [currentDraftType, setCurrentDraftType] = useState('');\n  const [currentDraftRef, setCurrentDraftRef] = useState('');\n  const [currentDraftMismatch, setCurrentDraftMismatch] = useState('');\n  const [currentDraftPortal, setCurrentDraftPortal] = useState('');\n  \n  // New Fetched Digital Notices State\n  const [digitalNotices, setDigitalNotices] = useState<any[]>([]);\n  const [selectedDigitalNotice, setSelectedDigitalNotice] = useState<any>(null);\n  const [draftViewMode, setDraftViewMode] = useState<'text' | 'pdf'>('text');\n\n  // Synchronize initial draft notice context when notices load\n  useEffect(() => {\n    if (!currentDraftClient && digitalNotices.length > 0) {\n      const firstNotice = digitalNotices[0];\n      setCurrentDraftClient(firstNotice.client);\n      setCurrentDraftType(firstNotice.portal === 'GST Portal' ? 'gst-show-cause' : firstNotice.portal === 'MCA Portal' ? 'mca-notice' : 'income-tax-response');\n      setCurrentDraftRef(firstNotice.refNumber);\n      setCurrentDraftMismatch(firstNotice.mismatchAmount);\n      setCurrentDraftPortal(firstNotice.portal);\n    }\n  }, [digitalNotices, currentDraftClient]);"

========================================
--- STEP 9887 (replace_file_content) ---
StartLine: 1088, EndLine: 1126
TargetContent:
"  // Fetch Client Deadlines — calculated dynamically from today's date\n  const fetchClientDeadlines = async () => {\n    const deadlines = getStatutoryDeadlines();\n    const mockClients = [\n      \"GlobalTrade India Logistics\",\n      \"Acme Technologies Pvt Ltd\",\n      \"TechVenture Solutions\",\n      \"GlobalTrade India Logistics\",\n      \"Acme Technologies Pvt Ltd\",\n      \"TechVenture Solutions\"\n    ];\n    const deadlinesWithClients = deadlines.map((d, index) => ({\n      ...d,\n      client: mockClients[index % mockClients.length]\n    }));\n    setClientDeadlines(deadlinesWithClients as any);\n    addAgentLog('📅 Statutory deadlines calculated for May–June 2026');\n  };\n\n  // Initialize Agent\n  useEffect(() => {\n    if (isAgentActive) {\n      addAgentLog('🤖 SANNIDH AI Agent initialized and monitoring...');\n      fetchRegulatoryNews();\n      fetchClientDeadlines();\n      \n      // Set up periodic monitoring\n      const monitoringInterval = setInterval(() => {\n        if (isAgentActive) {\n          addAgentLog('🔍 Scanning for compliance updates...');\n          fetchRegulatoryNews();\n          fetchClientDeadlines();\n        }\n      }, 60000); // Every minute\n      \n      return () => clearInterval(monitoringInterval);\n    }\n  }, [isAgentActive, addAgentLog]);"

ReplacementContent:
"  // Fetch Digital Notices dynamically from localStorage\n  const fetchDigitalNotices = () => {\n    let demoClients: any[] = [];\n    try {\n      const saved = localStorage.getItem('demo_clients');\n      if (saved) demoClients = JSON.parse(saved);\n    } catch (e) {}\n\n    if (demoClients.length === 0) {\n      setDigitalNotices([]);\n      return;\n    }\n\n    const notices: any[] = [];\n    demoClients.forEach((client, clientIndex) => {\n      const clientName = client.name || client.client_name;\n\n      // GST Notice\n      notices.push({\n        id: `not-dig-gst-${client.id || clientIndex}`,\n        refNumber: `SCN/DRC01/2026/${100 + clientIndex}`,\n        client: clientName,\n        portal: 'GST Portal',\n        noticeType: 'GST Show Cause Notice (GST-DRC-01)',\n        issueDate: '2026-02-07',\n        dueDate: '2026-06-25',\n        mismatchAmount: `₹${((10 + clientIndex * 4.3) * 100000).toLocaleString('en-IN')}`,\n        status: 'action_required'\n      });\n\n      // MCA Notice\n      notices.push({\n        id: `not-dig-mca-${client.id || clientIndex}`,\n        refNumber: `ROC/KA/ADJ/2026/${200 + clientIndex}`,\n        client: clientName,\n        portal: 'MCA Portal',\n        noticeType: 'MCA Adjudication Notice (ROC)',\n        issueDate: '2026-01-15',\n        dueDate: '2026-06-30',\n        mismatchAmount: `₹${((5 + clientIndex * 2.1) * 100000).toLocaleString('en-IN')}`,\n        status: 'action_required'\n      });\n\n      // Income Tax Notice\n      notices.push({\n        id: `not-dig-it-${client.id || clientIndex}`,\n        refNumber: `ITBA/AST/S/143(2)/2026-${27 + clientIndex}`,\n        client: clientName,\n        portal: 'Income Tax Portal',\n        noticeType: 'Section 143(2) Scrutiny Notice',\n        issueDate: '2026-06-05',\n        dueDate: '2026-07-10',\n        mismatchAmount: `₹${((15 + clientIndex * 7.7) * 100000).toLocaleString('en-IN')}`,\n        status: 'action_required'\n      });\n    });\n\n    setDigitalNotices(notices);\n  };\n\n  // Fetch Client D
<truncated 1851 bytes>

========================================
--- STEP 9891 (replace_file_content) ---
StartLine: 1617, EndLine: 1639
TargetContent:
"    const noticeKey = notice.id === 'not-dig-1' || notice.refNumber.includes('ZD07') ? 'gst-show-cause' :\n                      notice.id === 'not-dig-2' || notice.refNumber.includes('ROC') ? 'mca-notice' :\n                      notice.id === 'not-dig-3' || notice.refNumber.includes('ITBA') ? 'income-tax-response' : 'custom-draft';\n\n    setCurrentDraftClient(notice.client);\n    setCurrentDraftType(noticeKey);\n    setCurrentDraftRef(notice.refNumber);\n    setCurrentDraftPortal(notice.portal);\n    setCurrentDraftMismatch(notice.mismatchAmount);\n    setDraftApprovalStatus('pending');\n    setSignatureStatus(null);\n    setFilingARN(null);\n\n    const draftText = buildOfflineDraft({\n      documentType: noticeKey,\n      companyName: notice.client,\n      authority: notice.portal === \"GST Portal\" ? \"GST Department\" : notice.portal === \"MCA Portal\" ? \"ROC\" : \"Income Tax Department\",\n      noticeText: readyNoticeTemplates[noticeKey] || \"\",\n      modeLabel: \"conservative\",\n      templatePack: \"Detailed Para-Wise Reply\",\n      promptPack: \"Precedent Analysis + Objection Matrix\",\n      sovereignEngine: \"sannidh_sovereign\"\n    });"

ReplacementContent:
"    const noticeKey = notice.id?.includes('gst') || notice.refNumber?.includes('SCN') || notice.refNumber?.includes('ZD07') ? 'gst-show-cause' :\n                      notice.id?.includes('mca') || notice.refNumber?.includes('ROC') ? 'mca-notice' :\n                      notice.id?.includes('it') || notice.refNumber?.includes('ITBA') ? 'income-tax-response' : 'custom-draft';\n\n    setCurrentDraftClient(notice.client);\n    setCurrentDraftType(noticeKey);\n    setCurrentDraftRef(notice.refNumber);\n    setCurrentDraftPortal(notice.portal);\n    setCurrentDraftMismatch(notice.mismatchAmount);\n    setDraftApprovalStatus('pending');\n    setSignatureStatus(null);\n    setFilingARN(null);\n\n    const templateText = (readyNoticeTemplates[noticeKey] || \"\")\n      .replace(/GlobalTrade India Logistics/g, notice.client)\n      .replace(/Acme Technologies Pvt Ltd/g, notice.client)\n      .replace(/TechVenture Solutions/g, notice.client)\n      .replace(/₹18,46,920/g, notice.mismatchAmount)\n      .replace(/₹5,00,000/g, notice.mismatchAmount)\n      .replace(/₹27,80,000/g, notice.mismatchAmount)\n      .replace(/ZD070226019874A/g, notice.refNumber)\n      .replace(/ROC\\/KA\\/ADJ\\/2026\\/112/g, notice.refNumber)\n      .replace(/ITBA\\/AST\\/S\\/143\\(2\\)\\/2026-27/g, notice.refNumber);\n\n    const draftText = buildOfflineDraft({\n      documentType: noticeKey,\n      companyName: notice.client,\n      authority: notice.portal === \"GST Portal\" ? \"GST Department\" : notice.portal === \"MCA Portal\" ? \"ROC\" : \"Income Tax Department\",\n      noticeText: templateText,\n      modeLabel: \"conservative\",\n      templatePack: \"Detailed Para-Wise Reply\",\n      promptPack: \"Precedent Analysis + Objection Matrix\",\n      sovereignEngine: \"sannidh_sovereign\"\n    });"

========================================
--- STEP 9897 (replace_file_content) ---
StartLine: 1533, EndLine: 1559
TargetContent:
"      await new Promise(resolve => setTimeout(resolve, 1000));\n\n      const client = \"GlobalTrade India Logistics\";\n      const noticeKey = \"gst-show-cause\";\n      const portal = \"GST Portal\";\n      const refNum = \"ZD070226019874A\";\n      const mismatch = \"₹18,46,920\";\n\n      setCurrentDraftClient(client);\n      setCurrentDraftType(noticeKey);\n      setCurrentDraftRef(refNum);\n      setCurrentDraftPortal(portal);\n      setCurrentDraftMismatch(mismatch);\n      setDraftApprovalStatus('pending');\n      setSignatureStatus(null);\n      setFilingARN(null);\n\n      const draftText = buildOfflineDraft({\n        documentType: noticeKey,\n        companyName: client,\n        authority: \"GST Department\",\n        noticeText: readyNoticeTemplates[noticeKey] || \"\",\n        modeLabel: \"conservative\",\n        templatePack: \"Detailed Para-Wise Reply\",\n        promptPack: \"Precedent Analysis + Objection Matrix\",\n        sovereignEngine: \"sannidh_sovereign\"\n      });"

ReplacementContent:
"      let demoClients: any[] = [];\n      try {\n        const saved = localStorage.getItem('demo_clients');\n        if (saved) demoClients = JSON.parse(saved);\n      } catch (e) {}\n\n      if (demoClients.length === 0) {\n        toast.error(\"Please onboard a client in Client Portfolio first.\");\n        setIsProcessing(false);\n        return;\n      }\n\n      const client = demoClients[0].name || demoClients[0].client_name;\n      const noticeKey = \"gst-show-cause\";\n      const portal = \"GST Portal\";\n      const refNum = `SCN-GST-OCR-${Date.now().toString().slice(-4)}`;\n      const mismatch = \"₹12,45,670\";\n\n      setCurrentDraftClient(client);\n      setCurrentDraftType(noticeKey);\n      setCurrentDraftRef(refNum);\n      setCurrentDraftPortal(portal);\n      setCurrentDraftMismatch(mismatch);\n      setDraftApprovalStatus('pending');\n      setSignatureStatus(null);\n      setFilingARN(null);\n\n      const templateText = (readyNoticeTemplates[noticeKey] || \"\")\n        .replace(/GlobalTrade India Logistics/g, client)\n        .replace(/Acme Technologies Pvt Ltd/g, client)\n        .replace(/TechVenture Solutions/g, client)\n        .replace(/₹18,46,920/g, mismatch)\n        .replace(/₹5,00,000/g, mismatch)\n        .replace(/₹27,80,000/g, mismatch)\n        .replace(/ZD070226019874A/g, refNum)\n        .replace(/ROC\\/KA\\/ADJ\\/2026\\/112/g, refNum)\n        .replace(/ITBA\\/AST\\/S\\/143\\(2\\)\\/2026-27/g, refNum);\n\n      const draftText = buildOfflineDraft({\n        documentType: noticeKey,\n        companyName: client,\n        authority: \"GST Department\",\n        noticeText: templateText,\n        modeLabel: \"conservative\",\n        templatePack: \"Detailed Para-Wise Reply\",\n        promptPack: \"Precedent Analysis + Objection Matrix\",\n        sovereignEngine: \"sannidh_sovereign\"\n      });"

========================================
--- STEP 9903 (replace_file_content) ---
StartLine: 1760, EndLine: 1762
TargetContent:
"        `Client Name: ${currentDraftClient || 'GlobalTrade India Logistics'}`,"

ReplacementContent:
"        `Client Name: ${currentDraftClient || 'No Active Client'}`,"

========================================
--- STEP 9907 (replace_file_content) ---
StartLine: 2516, EndLine: 2518
TargetContent:
"                                <p className=\"font-serif italic text-xs font-bold text-blue-850\">{currentDraftClient.includes(\"GlobalTrade\") ? \"GlobalTrade Director\" : currentDraftClient.includes(\"Acme\") ? \"Acme Director\" : \"TechVenture Director\"}</p>"

ReplacementContent:
"                                <p className=\"font-serif italic text-xs font-bold text-blue-850\">{currentDraftClient || 'Client'} Director</p>"

========================================
--- STEP 9911 (replace_file_content) ---
StartLine: 2965, EndLine: 2970
TargetContent:
"                <div className=\"p-3 text-stone-800 font-mono font-medium\">\n                  {currentDraftClient.includes(\"GlobalTrade\") ? \"27AABCG5678K2ZQ\" : \n                   currentDraftClient.includes(\"Acme\") ? \"29AABCA1234Q1Z2\" : \"07AABCT9876E1Z4\"}\n                </div>"

ReplacementContent:
"                <div className=\"p-3 text-stone-800 font-mono font-medium\">\n                  {(() => {\n                    let demoClients: any[] = [];\n                    try {\n                      const saved = localStorage.getItem('demo_clients');\n                      if (saved) demoClients = JSON.parse(saved);\n                    } catch (e) {}\n                    const matched = demoClients.find(c => (c.name || c.client_name) === currentDraftClient);\n                    return matched?.gstin || matched?.cin || matched?.pan || \"N/A\";\n                  })()}\n                </div>"

========================================
--- STEP 9915 (replace_file_content) ---
StartLine: 3102, EndLine: 3154
TargetContent:
"  useEffect(() => {\n    // Seed default clients for premium CA Demo dashboard experience if empty\n    const saved = localStorage.getItem('demo_clients');\n    if (!saved || JSON.parse(saved).length === 0) {\n      const defaultClients = [\n        {\n          id: \"demo-client-1\",\n          name: \"GlobalTrade India Logistics\",\n          industry: \"Transportation & Logistics\",\n          gstin: \"27AABCG5678K2ZQ\",\n          pan: \"AABCG5678K\",\n          cin: \"U60230DL2021PTC389412\",\n          compliance_health: 78,\n          health: 78,\n          status: \"Active\",\n          risk_level: \"Medium\",\n          lastFiling: \"2026-05-15\",\n          pending_tasks: 2\n        },\n        {\n          id: \"demo-client-2\",\n          name: \"Acme Technologies Pvt Ltd\",\n          industry: \"Technology & Software\",\n          gstin: \"29AABCA1234Q1Z2\",\n          pan: \"AABCA1234Q\",\n          cin: \"U72900KA2022PTC159048\",\n          compliance_health: 92,\n          health: 92,\n          status: \"Active\",\n          risk_level: \"Low\",\n          lastFiling: \"2026-05-20\",\n          pending_tasks: 1\n        },\n        {\n          id: \"demo-client-3\",\n          name: \"TechVenture Solutions\",\n          industry: \"Professional Services\",\n          gstin: \"07AABCT9876E1Z4\",\n          pan: \"AABCT9876E\",\n          cin: \"U72200DL2023PTC390218\",\n          compliance_health: 64,\n          health: 64,\n          status: \"Active\",\n          risk_level: \"High\",\n          lastFiling: \"2026-05-10\",\n          pending_tasks: 3\n        }\n      ];\n      localStorage.setItem('demo_clients', JSON.stringify(defaultClients));\n      window.dispatchEvent(new CustomEvent('demo-client-added'));\n      window.dispatchEvent(new CustomEvent('ca:metrics-updated'));\n    }\n  }, []);"

ReplacementContent:
"  useEffect(() => {\n    // Check if demo_clients is in localStorage; if not, initialize as empty array\n    const saved = localStorage.getItem('demo_clients');\n    if (!saved) {\n      localStorage.setItem('demo_clients', JSON.stringify([]));\n    }\n    // Dispatch event to sync initial state across components\n    window.dispatchEvent(new CustomEvent('demo-client-added'));\n  }, []);"

========================================
--- STEP 9929 (replace_file_content) ---
StartLine: 3025, EndLine: 3031
TargetContent:
"const CADashboard = () => {\n  const navigate = useNavigate();\n  const { t, language, isRtlLayout, setLanguagePreference } = useLanguage();\n  const [dashboardOnline, setDashboardOnline] = useState(isOnline());\n\n  useEffect(() => {\n    const handleConn = () => setDashboardOnline(isOnline());"

ReplacementContent:
"const CADashboard = () => {\n  const navigate = useNavigate();\n  const { t, language, isRtlLayout, setLanguagePreference } = useLanguage();\n  const [dashboardOnline, setDashboardOnline] = useState(isOnline());\n  const [triggerUpdate, setTriggerUpdate] = useState(0);\n\n  useEffect(() => {\n    const handleUpdate = () => setTriggerUpdate(p => p + 1);\n    window.addEventListener('demo-client-added', handleUpdate);\n    return () => window.removeEventListener('demo-client-added', handleUpdate);\n  }, []);\n\n  useEffect(() => {\n    const handleConn = () => setDashboardOnline(isOnline());"

========================================
--- STEP 9935 (replace_file_content) ---
StartLine: 3227, EndLine: 3258
TargetContent:
"  // Update stats when metrics change\n  useEffect(() => {\n    if (metrics) {\n      setStats((prevStats) =>\n        prevStats.map((stat) => {\n          switch (stat.id) {\n            case \"companies\":\n              return { ...stat, value: String(metrics.assigned_companies ?? 0) };\n            case \"tasks\":\n              return { ...stat, value: String(metrics.active_tasks ?? 0) };\n            case \"due\":\n              return { ...stat, value: String(metrics.pending_filings_week ?? 0) };\n            case \"alerts\":\n              return { ...stat, value: String(metrics.high_risk_alerts ?? 0) };\n            case \"revenue\":\n              const rawRev = metrics.monthly_revenue ?? 0;\n              return {\n                ...stat,\n                value: `₹${(rawRev / 100000).toFixed(1)}L`,\n              };\n            case \"plan\":\n              return {\n                ...stat,\n                value: `${metrics.assigned_companies ?? 0}/10`,\n              };\n            default:\n              return stat;\n          }\n        })\n      );\n    }\n  }, [metrics]);"

ReplacementContent:
"  // Update stats dynamically based on actual demo clients and portal metrics\n  useEffect(() => {\n    let clientCount = 0;\n    try {\n      const saved = localStorage.getItem('demo_clients');\n      if (saved) clientCount = JSON.parse(saved).length;\n    } catch (e) {}\n\n    setStats((prevStats) =>\n      prevStats.map((stat) => {\n        switch (stat.id) {\n          case \"companies\":\n            return { ...stat, value: String(clientCount) };\n          case \"tasks\":\n            return { ...stat, value: String(metrics?.active_tasks ?? (clientCount * 2)) };\n          case \"due\":\n            return { ...stat, value: String(metrics?.pending_filings_week ?? Math.max(0, clientCount - 1)) };\n          case \"alerts\":\n            return { ...stat, value: String(metrics?.high_risk_alerts ?? (clientCount > 2 ? 1 : 0)) };\n          case \"revenue\":\n            const rawRev = metrics?.monthly_revenue ?? (clientCount * 25000);\n            return {\n              ...stat,\n              value: `₹${(rawRev / 1000).toFixed(0)}k`,\n            };\n          case \"plan\":\n            return {\n              ...stat,\n              value: `${clientCount}/10`,\n            };\n          default:\n            return stat;\n        }\n      })\n    );\n  }, [metrics, triggerUpdate]);"

========================================
--- STEP 9943 (replace_file_content) ---
StartLine: 2162, EndLine: 2191
TargetContent:
"                <div className=\"space-y-2\">\n                  {clientDeadlines.map((deadline) => (\n                    <div\n                      key={deadline.id}\n                      className={`flex items-center justify-between p-3 rounded-lg border ${\n                        deadline.status === 'urgent' ? 'bg-red-500/10 border-red-500/30' :\n                        deadline.status === 'overdue' ? 'bg-red-500/20 border-red-500/50' :\n                        'bg-card/20 border-cyan-500/20'\n                      }`}\n                    >\n                      <div>\n                        <p className=\"font-medium\">{deadline.client}</p>\n                        <p className=\"text-sm text-muted-foreground\">{deadline.type}</p>\n                      </div>\n                      <div className=\"flex items-center gap-3\">\n                        <Badge variant={deadline.status === 'urgent' ? 'destructive' : 'secondary'}>\n                          {deadline.daysRemaining} days\n                        </Badge>\n                        <Button\n                          size=\"sm\"\n                          variant=\"outline\"\n                          onClick={() => deployAgentForTask('compliance_check', deadline.client, `Prepare ${deadline.type}`)}\n                        >\n                          <Zap className=\"w-3 h-3 mr-1\" />\n                          Deploy AI\n                        </Button>\n                      </div>\n                    </div>\n                  ))}\n                </div>"

ReplacementContent:
"                <div className=\"space-y-2\">\n                  {clientDeadlines.length === 0 ? (\n                    <div className=\"p-6 rounded-xl border border-dashed border-border/50 text-center bg-card/10\">\n                      <Clock className=\"w-8 h-8 mx-auto mb-2 text-muted-foreground/50 animate-pulse\" />\n                      <p className=\"text-sm font-medium text-muted-foreground\">No statutory deadlines active</p>\n                      <p className=\"text-xs text-muted-foreground/60 mt-0.5\">Please add a client in the Client Portfolio below to start AI compliance monitoring.</p>\n                    </div>\n                  ) : (\n                    clientDeadlines.map((deadline) => (\n                      <div\n                        key={deadline.id}\n                        className={`flex items-center justify-between p-3 rounded-lg border ${\n                          deadline.status === 'urgent' ? 'bg-red-500/10 border-red-500/30' :\n                          deadline.status === 'overdue' ? 'bg-red-500/20 border-red-500/50' :\n                          'bg-card/20 border-cyan-500/20'\n                        }`}\n                      >\n                        <div>\n                          <p className=\"font-medium\">{deadline.client}</p>\n                          <p className=\"text-sm text-muted-foreground\">{deadline.type}</p>\n                        </div>\n                        <div className=\"flex items-center gap-3\">\n                          <Badge variant={deadline.status === 'urgent' ? 'destructive' : 'secondary'}>\n                            {deadline.daysRemaining} days\n                          </Badge>\n                          <Button\n                            size=\"sm\"\n                            variant=\"outline\"\n                            onClick={() => deployAgentForTask('compliance_check', deadline.client, `Prepare ${deadline.type}`)}\n                          >\n                            <Zap className=\"w-3 h-3 mr-1\" />\n         
<truncated 198 bytes>

========================================
--- STEP 9949 (replace_file_content) ---
StartLine: 2325, EndLine: 2375
TargetContent:
"                      <Badge className=\"bg-cyan-500/20 text-cyan-400 border-cyan-500/30\">3 New Notices</Badge>\n                    </div>\n                    <p className=\"text-xs text-muted-foreground\">\n                      Sannidh AI monitors GSTN, MCA-21, and CBDT servers under CA consent. The following notices require immediate response drafts.\n                    </p>\n                    <div className=\"space-y-3\">\n                      {digitalNotices.map((notice) => (\n                        <div\n                          key={notice.id}\n                          className={`p-4 rounded-xl border transition-all duration-300 bg-card/40 hover:bg-card/60 cursor-pointer ${\n                            selectedDigitalNotice?.id === notice.id\n                              ? 'border-cyan-500 shadow-[0_0_15px_rgba(6,182,212,0.15)] bg-cyan-950/10'\n                              : 'border-border/50'\n                          }`}\n                          onClick={() => setSelectedDigitalNotice(notice)}\n                        >\n                          <div className=\"flex items-start justify-between flex-wrap gap-2 mb-2\">\n                            <div>\n                              <div className=\"flex items-center gap-2 flex-wrap\">\n                                <span className=\"font-mono text-xs font-semibold text-cyan-300\">{notice.refNumber}</span>\n                                <Badge variant=\"outline\" className=\"text-[10px] uppercase border-cyan-500/30 text-cyan-400 bg-cyan-500/5\">\n                                  {notice.portal}\n                                </Badge>\n                              </div>\n                              <p className=\"text-sm font-medium text-white mt-1\">{notice.noticeType}</p>\n                              <p className=\"text-xs text-muted-foreground\">Client: {notice.client}</p>\n                            </div>\n                            <div className=\"text-right\">\n                              <span className=\"text-[
<truncated 1367 bytes>

ReplacementContent:
"                      <Badge className=\"bg-cyan-500/20 text-cyan-400 border-cyan-500/30\">\n                        {digitalNotices.length} New Notice{digitalNotices.length === 1 ? '' : 's'}\n                      </Badge>\n                    </div>\n                    <p className=\"text-xs text-muted-foreground\">\n                      Sannidh AI monitors GSTN, MCA-21, and CBDT servers under CA consent. The following notices require immediate response drafts.\n                    </p>\n                    <div className=\"space-y-3\">\n                      {digitalNotices.length === 0 ? (\n                        <div className=\"p-8 rounded-xl border border-dashed border-cyan-500/20 text-center bg-cyan-950/5\">\n                          <Database className=\"w-8 h-8 mx-auto mb-2 text-cyan-500/50 animate-pulse\" />\n                          <p className=\"text-sm font-medium text-cyan-400\">Portal Notices Inbox is Empty</p>\n                          <p className=\"text-xs text-zinc-400 mt-1 max-w-sm mx-auto\">No digital notices scraped yet. Onboard a client via consent to trigger secure GSP API sync.</p>\n                        </div>\n                      ) : (\n                        digitalNotices.map((notice) => (\n                          <div\n                            key={notice.id}\n                            className={`p-4 rounded-xl border transition-all duration-300 bg-card/40 hover:bg-card/60 cursor-pointer ${\n                              selectedDigitalNotice?.id === notice.id\n                                ? 'border-cyan-500 shadow-[0_0_15px_rgba(6,182,212,0.15)] bg-cyan-950/10'\n                                : 'border-border/50'\n                            }`}\n                            onClick={() => setSelectedDigitalNotice(notice)}\n                          >\n                            <div className=\"flex items-start justify-between flex-wrap gap-2 mb-2\">\n                              <div>\n                                <div className=\"flex items-center ga
<truncated 2243 bytes>

========================================
--- STEP 9985 (replace_file_content) ---
StartLine: 50, EndLine: 52
TargetContent:
"import { Progress } from \"@/components/ui/progress\";\nimport { Tabs, TabsContent, TabsList, TabsTrigger } from \"@/components/ui/tabs\";"

ReplacementContent:
"import { Progress } from \"@/components/ui/progress\";\nimport { Tabs, TabsContent, TabsList, TabsTrigger } from \"@/components/ui/tabs\";\nimport { Dialog, DialogContent, DialogHeader, DialogTitle, DialogDescription } from \"@/components/ui/dialog\";"

========================================
